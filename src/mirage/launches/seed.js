export default (dataServer) => {
    dataServer.create("launch");

    // upcoming launch
    dataServer.create("launch", "upcoming");
    dataServer.create("launch", "upcoming");
    dataServer.create("launch", "upcoming");
    dataServer.create("launch", "upcoming");
    dataServer.create("launch", "upcoming");
};