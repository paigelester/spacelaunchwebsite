import LaunchSeed from './seed';
import LaunchModels from './models';
import LaunchRoutes from './routes';
import LaunchFactories from './factories';

export default {
    seed: LaunchSeed,
    models: LaunchModels,
    routes: LaunchRoutes,
    factories: LaunchFactories
}